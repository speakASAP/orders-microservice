# ============================================
# Orders Configuration. Single Source of Truth
# ============================================
NODE_ENV=
DOMAIN=
SERVICE_NAME=

# Auth (JWT validation - same secret as auth-microservice)
JWT_SECRET=
AUTH_SERVICE_URL=

# Upstream flipflop order-service pricing backend (temporary during migration)
FLIPFLOP_ORDER_SERVICE_URL=
