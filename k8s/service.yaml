apiVersion: v1
kind: Service
metadata:
  name: orders-microservice
  namespace: statex-apps
  labels:
    app: orders-microservice
    managed-by: k8s-migration
spec:
  type: ClusterIP
  selector:
    app: orders-microservice
  ports:
    - protocol: TCP
      port: 3203
      targetPort: 3203
      name: http
